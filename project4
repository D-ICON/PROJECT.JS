//print a table containing multiplication table

for (let i = 1; i <= 5; i++) {
    for (let j = 1; j <= 5; j++) {
      let product = i * j;
      process.stdout.write(`${i} x ${j} = ${product}\t`);
    }
    console.log();
  }
  